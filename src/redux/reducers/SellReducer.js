const initialState = {
  sellUserDetails: {},
};

const SellReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default SellReducer;
